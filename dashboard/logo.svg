<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" fill="none" role="img" aria-label="Oberon">
  <!-- Monitoring orbit -->
  <ellipse cx="32" cy="33" rx="27" ry="10.5" transform="rotate(-28 32 33)"
          stroke="currentColor" stroke-width="2.5" opacity="0.5"/>
  <!-- Observed area / lens -->
  <circle cx="32" cy="34" r="15" stroke="currentColor" stroke-width="3"/>
  <!-- Precision reticle -->
  <circle cx="32" cy="34" r="4" stroke="currentColor" stroke-width="2.5"/>
  <path d="M32 13v5M32 49v5M13 34h5M47 34h5"
        stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/>
  <!-- Observation node (satellite) — focal point via fill -->
  <circle cx="55.5" cy="20" r="3.6" fill="currentColor"/>
</svg>
